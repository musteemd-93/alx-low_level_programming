#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 * @y: the character to be checked
 * Return: 1 if y is a letter, 0 otherwise
 */
int _isalpha(int y)
{
	return ((y >= 'a' && y <= 'z'));
}
